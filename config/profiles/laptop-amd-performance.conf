#!/usr/bin/env bash
# laptop-amd-performance.conf - Laptop with AMD GPU on AC power

# CPU performance with AMD pstate
if [[ -w /sys/devices/system/cpu/amd_pstate/status ]]; then
    echo "active" > /sys/devices/system/cpu/amd_pstate/status 2>/dev/null || true
fi

for cpu in /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor; do
    if [[ -w "$cpu" ]]; then
        echo "performance" > "$cpu" 2>/dev/null || true
    fi
done

# AMD GPU power profile
for card in /sys/class/drm/card*/device/power_dpm_force_performance_level; do
    if [[ -w "$card" ]]; then
        echo "high" > "$card" 2>/dev/null || true
    fi
done

# I/O scheduler
for device in /sys/block/nvme*/queue/scheduler; do
    if [[ -w "$device" ]]; then
        echo "none" > "$device" 2>/dev/null || true
    fi
done

echo "[profile] Laptop AMD performance profile applied"
